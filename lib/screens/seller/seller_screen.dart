import 'package:flutter/material.dart';

class SellerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Asih App - Seller'),
      ),
      body: Center(
        child: Text('Seller Screen'),
      ),
    );
  }
}