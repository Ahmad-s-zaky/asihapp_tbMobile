import 'package:flutter/material.dart';

class ProduksiScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Asih App - Produksi'),
      ),
      body: Center(
        child: Text('Produksi Screen'),
      ),
    );
  }
}
